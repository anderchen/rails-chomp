  <%= simple_form_for([ product, review ], remote: true) do |form| %>
  <%= form.input :approved, as: :radio_buttons, label: 'As informações estão corretos?' %>
  <%= form.input :content, as: :text %>
  <%= form.button :submit %>
<% end %>
