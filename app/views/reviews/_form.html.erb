  <%= simple_form_for([ product, review ], remote: true) do |form| %>
  <%= form.input :approved, as: :radio_buttons, label: 'As informações acima estão corretas?' %>
  <%= form.input :content, label: "Diga o que você achou:", as: :text %>
  <%= form.submit "Enviar", class: "btn btn-primary" %>
<% end %>
